NODE_ENV=development
# Match the known deployment topology: false, a hop count, or proxy IP/CIDR values.
# Never use true; do not guess the proxy topology.
TRUST_PROXY=false
PORT=3000
CLIENT_URL=http://localhost:5173
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=riona_cafe_menu
